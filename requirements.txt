# Fish Audio S2 - ComfyUI custom nodes
#
# fish-speech is bundled in fish_speech_src/
# auto-gptq is bundled in auto_gptq_src/
# Only runtime deps are installed here. torch is intentionally excluded.
#
# DO NOT add torch/torchaudio here — ComfyUI manages those.

soundfile
loguru
transformers>=4.45.2
einops>=0.7.0
librosa>=0.10.1
rich>=13.5.3
ormsgpack
pydantic==2.9.2
tiktoken>=0.8.0
cachetools
zstandard>=0.22.0
resampy>=0.4.3
protobuf>=4.21.0
safetensors>=0.4.0
pyrootutils>=1.0.4
